# Claude Instructions For `apps/voice`

Read and follow `AGENTS.md` in this directory first. It is the source of truth for the voice app's target architecture, Deepgram notes, AnkiMCP contract, secrets handling, UX requirements, and browser-debugging rules.

The current code is a working browser-demo voice agent, not placeholder scaffolding. Preserve useful Deepgram Voice Agent, UI, and demo-review behavior unless the task intentionally replaces it with the server-routed AnkiMCP flow described in `AGENTS.md`.
